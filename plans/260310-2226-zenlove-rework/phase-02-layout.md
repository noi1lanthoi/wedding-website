# Phase 02: Global Layout & Sticky Bar

Status: ✅ Complete
Dependencies: Phase 01

## Objective

Xây dựng khung dàn trang tổng thể theo chuẩn Mobile-First. Đặc biệt là làm thanh tiện ích gắn dưới đáy màn hình (Sticky Bottom Bar) và bong bóng lời chúc thả nổi đặc trưng của Zenlove (Floating Wishes).

## Implementation Steps

1. [x] Tạo Component `StickyBottomBar.tsx` (có nút Gửi Lời chúc, RSVP, Cài đặt âm thanh). Cố định `bottom-0`.
2. [x] Tạo Component `FloatingWishes.tsx` hiển thị random các dòng chữ ngắn gọn chạy từ dưới lên phía mép trái màn hình.
3. [x] Thêm 2 components này vào `src/app/page.tsx`.

## Files to Create/Modify

- `src/components/StickyBottomBar.tsx` (NEW)
- `src/components/FloatingWishes.tsx` (NEW)
- `src/app/page.tsx`

---

Next Phase: Phase 03: Envelope & Hero Section
